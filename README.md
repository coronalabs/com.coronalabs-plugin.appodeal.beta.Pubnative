Sources for the plugin `plugin.appodeal.beta.Pubnative`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Pubnative" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
